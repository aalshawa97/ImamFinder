package com.example.detailapplication.placeholder

import android.view.View
import com.google.android.material.snackbar.Snackbar
class Utils
{
    fun View.snackbar()
    {
        /*
        Snackbar.make(
            view: this,
            message,
            Snackbar.LENGTH_LONG
        ).also{ snackbar ->snackbar.setAction("Ok") {snackbar.dismiss()}
        */
    }
}